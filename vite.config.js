/**
 * ============================================================
 *  Vite config
 * ============================================================
 *  📚 درس — Vite: خادم تطوير وخادم بناء سريع.
 *  - root: مجلد جذر المشروع (يحتوي index.html) — الافتراضي.
 *  - base: './' حتى تعمل المسارات النسبية عند بناء المجلد
 *    وإخراجه لأي استضافة.
 */
import { defineConfig } from 'vite';

export default defineConfig({
    base: './',
    server: {
        host: true,          // يُتيح فتح اللعبة من هاتف على نفس الشبكة
    },
});
